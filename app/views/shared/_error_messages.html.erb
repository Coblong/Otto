<% if obj.errors.any? %>
    <div class="alert alert-error">
      The form contains <%= pluralize("error", obj.errors.count) %>.    
    <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>